{
  "name": "chatapp",
  "version": "0.2.0",
  "description": "",
  "main": "./backend/src/main.ts",
  "scripts": {
    "install": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "deploy": "NODE_VERSION=18 NPM_VERSION=8 netlify deploy",
    "start": "npm run start",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "global-start": "npm run install && npm run build && npm run start"
  },
  "keywords": [],
  "author": "Arman",
  "engines": {
    "node": "16"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "debug": "^4.3.4",
    "depd": "^2.0.0",
    "encodeurl": "^1.0.2",
    "escape-html": "^1.0.3",
    "express": "^4.18.2",
    "finalhandler": "^1.2.0",
    "merge-descriptors": "^1.0.1",
    "ms": "^2.1.3",
    "netlify": "^13.1.9",
    "netlify-cli": "^15.7.0",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0"
  }
}
