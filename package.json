{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "",
  "main": "./backend/src/main.ts",
  "scripts": {
     "install": "cd ./backend && npm install --omit=dev && cd ../frontend && npm install && cd ../",
     "start": "cd ./backend && npm run start",
     "build": "cd ./frontend && npm run build && cd ../",
     "global-start": "npm run install && npm run build && npm run start"
  },
  "keywords": [],
  "author": "Arman",
  "engines": {
    "node": "16"
  }
}
